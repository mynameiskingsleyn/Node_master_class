import { MapFieldPipe } from './map-field.pipe';

describe('MapFieldPipe', () => {
  it('create an instance', () => {
    const pipe = new MapFieldPipe();
    expect(pipe).toBeTruthy();
  });
});
